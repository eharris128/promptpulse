{
  "name": "promptpulse-docs",
  "version": "1.0.0",
  "description": "Documentation site for PromptPulse",
  "private": true,
  "scripts": {
    "build": "hugo --minify",
    "serve": "hugo server --buildDrafts",
    "dev": "hugo server --buildDrafts --watch"
  },
  "devDependencies": {
    "postcss": "^8.4.47",
    "postcss-cli": "^11.0.0",
    "autoprefixer": "^10.4.20"
  }
}